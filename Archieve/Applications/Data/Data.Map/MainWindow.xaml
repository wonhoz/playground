<Window x:Class="DataMap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Data.Map" Height="720" Width="1200"
        Background="#0F0F1E" Foreground="#E0E0E0"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#252538"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#444466"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353555"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#444466"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E0E0E0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#252538"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#444466"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Padding="{TemplateBinding Padding}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="16"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                    Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                    HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="▾" Foreground="#888888"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom" AllowsTransparency="True">
                                <Border Background="#252538" BorderBrush="#444466" BorderThickness="1" CornerRadius="4">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#E0E0E0"/>
                        <Setter Property="Padding" Value="8,4"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353555"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A2A50"/>
                                <Setter Property="Foreground" Value="#50C8DC"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="14" Height="14" BorderBrush="#444466" BorderThickness="1"
                                    Background="#1E1E30" CornerRadius="2" VerticalAlignment="Center">
                                <Path x:Name="CheckMark" Data="M2,6 L5,9 L11,3"
                                      Stroke="#50C8DC" StrokeThickness="2" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <!-- 툴바 -->
        <Border Grid.Row="0" Background="#14142A" BorderBrush="#1E1E35" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="🗺 Data.Map" FontSize="14" FontWeight="Bold"
                           Foreground="#50C8DC" Margin="0,0,20,0"/>
                <TextBlock Text="GeoJSON / TopoJSON / CSV 오프라인 인터랙티브 지도 시각화기" FontSize="11"
                           Foreground="#666688" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- 메인 레이아웃 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 왼쪽 패널 -->
            <Border Grid.Column="0" BorderBrush="#333355" BorderThickness="1" CornerRadius="6" Margin="6,6,3,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="44"/>
                    </Grid.RowDefinitions>

                    <Border Background="#1A1A30" CornerRadius="6,6,0,0">
                        <TextBlock Text="데이터 파일" FontSize="11" Foreground="#7777AA"
                                   VerticalAlignment="Center" Margin="8,0"/>
                    </Border>

                    <!-- 파일 선택 -->
                    <Border Grid.Row="1" Padding="8,8" Background="#14142A">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Button x:Name="BtnBrowse" Click="BtnBrowse_Click"
                                        Padding="8,3" FontSize="11">📂 파일 열기</Button>
                                <Button x:Name="BtnClear" Click="BtnClear_Click"
                                        Margin="6,0,0,0" Padding="8,3" FontSize="11">✕ 지우기</Button>
                            </StackPanel>
                            <TextBox x:Name="TxtFilePath" FontSize="10" FontFamily="Consolas"
                                     IsReadOnly="True" Padding="4,2" Height="40"
                                     AcceptsReturn="True" TextWrapping="Wrap"/>
                            <TextBlock x:Name="LblFileType" Text="형식: —" FontSize="10"
                                       Foreground="#666688" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- CSV 전용 옵션 -->
                    <Border x:Name="PanelCsvOptions" Grid.Row="2" Padding="8,6" Background="#16162C"
                            Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="CSV 옵션" FontSize="10" Foreground="#7777AA" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="색상 필드:" FontSize="10" Foreground="#AAAACC" Width="70" VerticalAlignment="Center"/>
                                <ComboBox x:Name="CmbColorField" Width="150" FontSize="11"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="크기 필드:" FontSize="10" Foreground="#AAAACC" Width="70" VerticalAlignment="Center"/>
                                <ComboBox x:Name="CmbSizeField" Width="150" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- GeoJSON 옵션 -->
                    <Border x:Name="PanelGeoOptions" Grid.Row="3" Padding="8,6" Background="#16162C"
                            Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="GeoJSON 옵션" FontSize="10" Foreground="#7777AA" Margin="0,0,0,6"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="색상 필드:" FontSize="10" Foreground="#AAAACC" Width="70" VerticalAlignment="Center"/>
                                <ComboBox x:Name="CmbGeoColorField" Width="150" FontSize="11"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="값 필드:" FontSize="10" Foreground="#AAAACC" Width="70" VerticalAlignment="Center"/>
                                <ComboBox x:Name="CmbGeoValueField" Width="150" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 필드 목록 -->
                    <Border Grid.Row="4" BorderThickness="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="22"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Background="#16162E" Padding="8,3">
                                <TextBlock x:Name="LblDataInfo" Text="데이터 없음" FontSize="10" Foreground="#666688"/>
                            </Border>
                            <ListBox x:Name="LstFields" Grid.Row="1" FontFamily="Consolas" FontSize="10"
                                     Background="#13132A" BorderThickness="0" Foreground="#AAAACC"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="6,2"/>
                                        <Setter Property="Foreground" Value="#AAAACC"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>
                        </Grid>
                    </Border>

                    <!-- 렌더링 버튼 -->
                    <Border Grid.Row="5" Background="#14142A" Padding="8,8">
                        <Button x:Name="BtnRender" Click="BtnRender_Click"
                                Padding="0,6" FontSize="12"
                                Background="#1A3A2A" BorderBrush="#2A6A4A">🗺 지도에 표시</Button>
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" Background="#1A1A30" HorizontalAlignment="Stretch"/>

            <!-- 지도 WebView2 -->
            <Border Grid.Column="2" BorderBrush="#333355" BorderThickness="1" CornerRadius="6" Margin="3,6,6,6">
                <wv2:WebView2 x:Name="WebMap"/>
            </Border>
        </Grid>

        <!-- 상태바 -->
        <Border Grid.Row="2" Background="#0A0A1A" BorderBrush="#1A1A30" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusBar" Margin="10,0" VerticalAlignment="Center"
                       FontSize="10" Foreground="#666688"/>
        </Border>
    </Grid>
</Window>
