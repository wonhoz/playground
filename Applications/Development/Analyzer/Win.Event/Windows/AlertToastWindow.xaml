<Window x:Class="WinEvent.Windows.AlertToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="360" Height="110"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Loaded="Window_Loaded">

    <Border Background="#1C1010" BorderBrush="#FF4040" BorderThickness="1,1,1,3"
            CornerRadius="8" Padding="14,10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       x:Name="TxtRule"
                       FontSize="12" FontWeight="SemiBold"
                       Foreground="#FF6060"/>
            <Button Grid.Row="0" Grid.Column="1"
                    Content="✕" Width="20" Height="20"
                    Background="Transparent" Foreground="#888898"
                    BorderThickness="0" Cursor="Hand" FontSize="11"
                    Click="BtnClose_Click" VerticalAlignment="Top"/>

            <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                       x:Name="TxtInfo"
                       Foreground="#C0C0C0" FontSize="11"/>

            <TextBlock Grid.Row="4" Grid.ColumnSpan="2"
                       x:Name="TxtMsg"
                       Foreground="#A0A0A0" FontSize="11"
                       TextTrimming="CharacterEllipsis"/>
        </Grid>
    </Border>
</Window>
