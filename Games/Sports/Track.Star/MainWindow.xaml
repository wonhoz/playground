<Window x:Class="TrackStar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Track.Star" Width="800" Height="500"
        ResizeMode="NoResize"
        Background="#1A1A2E" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown" KeyUp="Window_KeyUp"
        Focusable="True">
    <Grid>
        <Canvas x:Name="GameCanvas" Background="Transparent" ClipToBounds="True"/>

        <!-- HUD -->
        <Grid x:Name="HudPanel" VerticalAlignment="Top" Margin="16,8" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="EventNameText" Grid.Column="0" Text="" FontSize="16" FontWeight="Bold"
                       Foreground="#FFD700" FontFamily="Consolas"/>
            <TextBlock x:Name="TimerText" Grid.Column="1" Text="" FontSize="20" FontWeight="Bold"
                       Foreground="#00FFCC" HorizontalAlignment="Center" FontFamily="Consolas"/>
            <TextBlock x:Name="InfoText" Grid.Column="2" Text="" FontSize="14"
                       Foreground="#888888" HorizontalAlignment="Right" FontFamily="Consolas"/>
        </Grid>

        <!-- 게이지 바 (파워/각도용) -->
        <StackPanel x:Name="GaugePanel" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                    Margin="0,0,0,30" Visibility="Collapsed">
            <TextBlock x:Name="GaugeLabel" Text="POWER" FontSize="12" FontWeight="Bold"
                       Foreground="#FFD700" HorizontalAlignment="Center" FontFamily="Consolas"/>
            <Border Background="#333350" CornerRadius="4" Height="18" Width="300" Margin="0,4,0,0">
                <Canvas ClipToBounds="True">
                    <Border x:Name="GaugeFill" Background="#FF6600" CornerRadius="4"
                            Height="18" Width="0"/>
                    <Border x:Name="GaugeOptimal" Background="#2ECC7166" Width="40" Height="18"
                            Canvas.Left="120"/>
                </Canvas>
            </Border>
        </StackPanel>

        <!-- 타이틀 -->
        <StackPanel x:Name="TitlePanel" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="🏅 TRACK.STAR" FontSize="44" FontWeight="Bold"
                       Foreground="#FFD700" HorizontalAlignment="Center" FontFamily="Consolas">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#FF8800" BlurRadius="25" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="OLYMPIC TRACK &amp; FIELD" FontSize="13" Foreground="#888888"
                       HorizontalAlignment="Center" Margin="0,4,0,0" FontFamily="Consolas"/>
            <TextBlock Text="[ ENTER 키로 대회 시작 ]" FontSize="16" Foreground="#00FFCC"
                       HorizontalAlignment="Center" Margin="0,32,0,0" FontFamily="Consolas">
                <TextBlock.Effect>
                    <DropShadowEffect Color="#00FFCC" BlurRadius="10" ShadowDepth="0"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock FontSize="12" Foreground="#666666" HorizontalAlignment="Center"
                       Margin="0,16,0,0" FontFamily="Consolas" TextAlignment="Center">
                <Run Text="종목: 100m · 허들 · 멀리뛰기 · 창던지기"/>
                <LineBreak/>
                <Run Text="←→  달리기    SPACE  점프/던지기"/>
            </TextBlock>
        </StackPanel>

        <!-- 카운트다운 -->
        <TextBlock x:Name="CountdownText" Text="" FontSize="64" FontWeight="Bold"
                   Foreground="#FFD700" HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontFamily="Consolas" Visibility="Collapsed">
            <TextBlock.Effect>
                <DropShadowEffect Color="#FF8800" BlurRadius="20" ShadowDepth="0"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- 결과 -->
        <Border x:Name="ResultOverlay" Background="#CC000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="ResultTitle" Text="" FontSize="28" FontWeight="Bold"
                           Foreground="#FFD700" HorizontalAlignment="Center" FontFamily="Consolas"/>
                <TextBlock x:Name="ResultDetail" Text="" FontSize="18"
                           Foreground="#00FFCC" HorizontalAlignment="Center" Margin="0,12,0,0" FontFamily="Consolas"/>
                <TextBlock x:Name="ResultNext" Text="" FontSize="14" Foreground="#888888"
                           HorizontalAlignment="Center" Margin="0,24,0,0" FontFamily="Consolas"/>
            </StackPanel>
        </Border>

        <!-- 최종 결과 -->
        <Border x:Name="FinalOverlay" Background="#CC000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="★ 대회 종료 ★" FontSize="36" FontWeight="Bold"
                           Foreground="#FFD700" HorizontalAlignment="Center" FontFamily="Consolas">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FFD700" BlurRadius="20" ShadowDepth="0"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock x:Name="FinalMedals" Text="" FontSize="16"
                           Foreground="#E0E0E0" HorizontalAlignment="Center" Margin="0,16,0,0"
                           FontFamily="Consolas" TextAlignment="Center"/>
                <TextBlock Text="[ ENTER 키로 재시작 ]" FontSize="14" Foreground="#888888"
                           HorizontalAlignment="Center" Margin="0,24,0,0" FontFamily="Consolas"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
