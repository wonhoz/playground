<Window x:Class="Stock.Fetch.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KIS Open API 키 설정" Height="370" Width="520"
        Background="{StaticResource WindowBg}"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20" Background="{StaticResource WindowBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="한국투자증권 OpenAPI 키"
                   FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="{StaticResource FgMuted}" Margin="0,0,0,16"
                   Text="KIS 소스를 쓸 때만 필요합니다. 네이버·Yahoo·KRX 소스는 키 없이 동작합니다. 키는 이 PC의 %LocalAppData%에만 저장되며 저장소에 포함되지 않습니다."/>

        <TextBlock Grid.Row="2" Text="APP KEY" Margin="0,0,0,4" Foreground="{StaticResource FgMuted}"/>
        <TextBox x:Name="KeyBox" Grid.Row="2" Margin="0,22,0,12"/>

        <TextBlock Grid.Row="3" Text="APP SECRET" Margin="0,0,0,4" Foreground="{StaticResource FgMuted}"/>
        <PasswordBox x:Name="SecretBox" Grid.Row="3" Margin="0,22,0,12"/>

        <CheckBox x:Name="MockCheck" Grid.Row="4" Content="모의투자 서버 사용 (테스트용 · 시세는 실전 권장)"
                  Margin="0,4,0,0"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="취소" Width="90" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="저장" Width="90" Style="{StaticResource AccentButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
