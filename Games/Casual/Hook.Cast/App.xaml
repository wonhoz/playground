<Application x:Class="HookCast.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- 낚시 테마 팔레트 -->
        <SolidColorBrush x:Key="SkyBrush"    Color="#1A2A4A"/>
        <SolidColorBrush x:Key="WaterBrush"  Color="#0A3050"/>
        <SolidColorBrush x:Key="DeepBrush"   Color="#051828"/>
        <SolidColorBrush x:Key="UiBgBrush"   Color="#0E1620"/>
        <SolidColorBrush x:Key="UiPanelBrush" Color="#1A2A3A"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2E4060"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#3DB8FF"/>
        <SolidColorBrush x:Key="GoldBrush"   Color="#FFD060"/>
        <SolidColorBrush x:Key="GreenBrush"  Color="#50DC78"/>
        <SolidColorBrush x:Key="FgBrush"     Color="#D0E8FF"/>
        <SolidColorBrush x:Key="Fg2Brush"    Color="#7090A8"/>

        <Style TargetType="Button">
            <Setter Property="Background"      Value="#1A3050"/>
            <Setter Property="Foreground"      Value="{StaticResource FgBrush}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="14,7"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2A4060"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#0A2040"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
