<Window x:Class="Photo.Video.Organizer.PreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="미리 보기"
        Width="520" Height="460"
        MinHeight="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">

    <Border Background="#2D2D2D" CornerRadius="10" BorderBrush="#404040" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="정리 미리 보기"
                               FontSize="15" FontWeight="SemiBold" Foreground="#EEEEEE"/>
                    <TextBlock x:Name="SubtitleText"
                               Text=""
                               FontSize="11" Foreground="#9E9E9E" Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="✕" Click="Close_Click"
                        Width="28" Height="28" Cursor="Hand"
                        Foreground="#9E9E9E" BorderThickness="0" Background="Transparent">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="2">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#E81123"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Summary Bar -->
            <Border Grid.Row="1" Background="#1A4FC3F7" CornerRadius="6" Padding="10,7" Margin="0,0,0,10">
                <TextBlock x:Name="SummaryText"
                           FontSize="11" Foreground="#4FC3F7" TextWrapping="Wrap"/>
            </Border>

            <!-- Group List -->
            <Border Grid.Row="2" Background="#252525" CornerRadius="6">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="GroupList" Margin="8">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,2" Padding="8,6" CornerRadius="4"
                                        Background="#2D2D2D">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding FolderDisplay}"
                                                       FontSize="12" FontWeight="SemiBold"
                                                       Foreground="#EEEEEE"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Text="{Binding FileListDisplay}"
                                                       FontSize="10" Foreground="#777777"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Border Grid.Column="1"
                                                Background="#4FC3F7" CornerRadius="10"
                                                Padding="8,2" VerticalAlignment="Center" Margin="8,0,0,0">
                                            <TextBlock Text="{Binding CountDisplay}"
                                                       FontSize="11" FontWeight="SemiBold"
                                                       Foreground="#1E1E1E"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Footer -->
            <Button Grid.Row="3" Content="닫기" Click="Close_Click"
                    HorizontalAlignment="Right" Margin="0,14,0,0"
                    Cursor="Hand" Padding="24,8">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#4FC3F7"/>
                        <Setter Property="Foreground" Value="#1E1E1E"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#29B6F6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
