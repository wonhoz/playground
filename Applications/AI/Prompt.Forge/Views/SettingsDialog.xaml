<Window x:Class="Prompt.Forge.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="동기화 설정" Width="640" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A2A" Foreground="#E0E0E0"
        ResizeMode="NoResize"
        FontFamily="Segoe UI" FontSize="13">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background"       Value="#252535"/>
            <Setter Property="Foreground"       Value="#E0E0E0"/>
            <Setter Property="BorderBrush"      Value="#3A3A55"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="8,6"/>
            <Setter Property="CaretBrush"       Value="#7C3AED"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#7C3AED"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background"    Value="#2A2A3E"/>
            <Setter Property="Foreground"    Value="#C0C0D0"/>
            <Setter Property="BorderBrush"   Value="#3A3A55"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"       Value="12,6"/>
            <Setter Property="Cursor"        Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#353550"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6D28D9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#A0A0BB"/>
            <Setter Property="Padding"    Value="0"/>
            <Setter Property="Margin"     Value="0,0,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 방법 1: DB 경로 -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="방법 1 — 클라우드 드라이브 (OneDrive / Google Drive / Dropbox)"
                       FontSize="14" FontWeight="SemiBold" Foreground="#7C3AED"
                       TextTrimming="CharacterEllipsis" Margin="0,0,0,8"/>
            <TextBlock Foreground="#888899" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"
                       Text="DB 파일 경로를 클라우드 드라이브 폴더로 지정하면 자동 동기화됩니다. 변경 후 재시작 필요. (동시에 두 PC에서 열지 마세요)"/>
            <TextBlock Text="프롬프트 DB 파일 경로 (비워두면 기본값)" Foreground="#A0A0BB"
                       FontSize="12" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="TxtDbPath" Height="34"/>
                <Button  Grid.Column="2" Content="찾아보기" Height="34" Click="Browse_Click"/>
                <Button  Grid.Column="4" Content="기본값" Height="34" Click="ResetDbPath_Click"/>
            </Grid>
            <TextBlock x:Name="TxtCurrentPath" Foreground="#555566" FontSize="10"
                       TextTrimming="CharacterEllipsis" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- 구분선 -->
        <Separator Grid.Row="1" Background="#2A2A3E" VerticalAlignment="Center"/>

        <!-- 방법 2: GitHub Gist -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="방법 2 — GitHub Gist (JSON 수동 동기화)"
                       FontSize="14" FontWeight="SemiBold" Foreground="#7C3AED"
                       TextTrimming="CharacterEllipsis" Margin="0,0,0,8"/>
            <TextBlock Foreground="#888899" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"
                       Text="GitHub PAT를 입력하면 툴바 ☁ 버튼으로 Gist 업로드/다운로드가 가능합니다. PAT 권한: gist (read/write)"/>
            <TextBlock Text="GitHub Personal Access Token (PAT)" Foreground="#A0A0BB"
                       FontSize="12" Margin="0,0,0,6"/>
            <TextBox x:Name="TxtPat" Height="34" Margin="0,0,0,12"/>
            <TextBlock Text="Gist ID (처음 업로드 시 자동 생성·저장)" Foreground="#A0A0BB"
                       FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,0,0,6"/>
            <TextBox x:Name="TxtGistId" Height="34"/>
        </StackPanel>

        <!-- 하단 버튼 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Click="Cancel_Click" Margin="0,0,8,0"/>
            <Button Content="저장" Style="{StaticResource AccentBtn}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
