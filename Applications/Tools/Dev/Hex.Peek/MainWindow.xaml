<Window x:Class="HexPeek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:HexPeek.Models"
        Title="Hex.Peek" Height="720" Width="1100"
        MinHeight="500" MinWidth="820"
        Background="#1A1A1A" Foreground="#E0E0E0"
        AllowDrop="True"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <!-- ── 색상 팔레트 ───────────────────────────────────────────────── -->
        <SolidColorBrush x:Key="OffsetBrush"    Color="#6272A4"/>
        <SolidColorBrush x:Key="HexBrush"       Color="#CDD6F4"/>
        <SolidColorBrush x:Key="AsciiBrush"     Color="#F1FA8C"/>
        <SolidColorBrush x:Key="AccentBrush"    Color="#0097C8"/>
        <SolidColorBrush x:Key="ToolbarBrush"   Color="#252526"/>
        <SolidColorBrush x:Key="BorderColor"    Color="#333333"/>

        <!-- ── 툴바 버튼 ─────────────────────────────────────────────────── -->
        <Style x:Key="ToolBtn" TargetType="Button">
            <Setter Property="Background"       Value="#2D2D2D"/>
            <Setter Property="Foreground"       Value="#E0E0E0"/>
            <Setter Property="BorderBrush"      Value="#444444"/>
            <Setter Property="BorderThickness"  Value="1"/>
            <Setter Property="Padding"          Value="12,5"/>
            <Setter Property="FontSize"         Value="12"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3D3D3D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4D4D4D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentBtn" TargetType="Button" BasedOn="{StaticResource ToolBtn}">
            <Setter Property="Background" Value="#0097C8"/>
            <Setter Property="BorderBrush" Value="#0097C8"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00AADD"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#007EA8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ── 검색 TextBox ──────────────────────────────────────────────── -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Background"   Value="#2A2A2A"/>
            <Setter Property="Foreground"   Value="#E0E0E0"/>
            <Setter Property="BorderBrush"  Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"      Value="8,5"/>
            <Setter Property="CaretBrush"   Value="#E0E0E0"/>
            <Setter Property="FontFamily"   Value="Cascadia Code,Consolas"/>
            <Setter Property="FontSize"     Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="Hidden"
                                          HorizontalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#0097C8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── 헥스 행 ListBoxItem ──────────────────────────────────────── -->
        <Style x:Key="HexItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background"              Value="Transparent"/>
            <Setter Property="Padding"                 Value="0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E2A44"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected"  Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="#222222"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── 헥스 행 데이터 템플릿 ────────────────────────────────────── -->
        <DataTemplate x:Key="HexRowTemplate" DataType="{x:Type models:HexRow}">
            <Grid Margin="8,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75"/>   <!-- Offset -->
                    <ColumnDefinition Width="16"/>   <!-- gap -->
                    <ColumnDefinition Width="Auto"/> <!-- Hex -->
                    <ColumnDefinition Width="12"/>   <!-- separator -->
                    <ColumnDefinition Width="Auto"/> <!-- ASCII -->
                </Grid.ColumnDefinitions>

                <!-- 오프셋 -->
                <TextBlock Grid.Column="0"
                           Text="{Binding OffsetText}"
                           Foreground="{StaticResource OffsetBrush}"
                           FontFamily="Cascadia Code,Consolas" FontSize="13"/>

                <!-- HEX 바이트 -->
                <TextBlock Grid.Column="2"
                           Text="{Binding HexText}"
                           Foreground="{StaticResource HexBrush}"
                           FontFamily="Cascadia Code,Consolas" FontSize="13"/>

                <!-- 구분선 -->
                <TextBlock Grid.Column="3"
                           Text="│"
                           Foreground="#3A3A3A"
                           HorizontalAlignment="Center"
                           FontFamily="Cascadia Code,Consolas" FontSize="13"/>

                <!-- ASCII -->
                <TextBlock Grid.Column="4"
                           Text="{Binding AsciiText}"
                           Foreground="{StaticResource AsciiBrush}"
                           FontFamily="Cascadia Code,Consolas" FontSize="13"/>
            </Grid>
        </DataTemplate>

    </Window.Resources>

    <!-- ── 전체 레이아웃 ─────────────────────────────────────────────────── -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 툴바 -->
            <RowDefinition Height="Auto"/> <!-- 컬럼 헤더 -->
            <RowDefinition Height="*"/>    <!-- 헥스 뷰 + 구조체 패널 -->
            <RowDefinition Height="Auto"/> <!-- 디코드 패널 -->
            <RowDefinition Height="Auto"/> <!-- 상태바 -->
        </Grid.RowDefinitions>

        <!-- ── 툴바 ─────────────────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="{StaticResource ToolbarBrush}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 왼쪽: 파일 관련 버튼 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="BtnOpen"    Content="📂 열기"    Style="{StaticResource ToolBtn}" Click="BtnOpen_Click"/>
                    <Button x:Name="BtnSave"    Content="💾 저장"    Style="{StaticResource ToolBtn}" Click="BtnSave_Click" IsEnabled="False" Margin="4,0,0,0"/>
                    <Rectangle Width="1" Fill="#444444" Margin="4,2"/>
                    <Button x:Name="BtnCompare" Content="⇄ 비교 열기" Style="{StaticResource ToolBtn}" Click="BtnCompare_Click" Margin="4,0,0,0"/>
                    <Button x:Name="BtnGoto"    Content="↷ 오프셋 이동 (Ctrl+G)" Style="{StaticResource ToolBtn}" Click="BtnGoto_Click" Margin="4,0,0,0"/>
                    <Rectangle Width="1" Fill="#444444" Margin="4,2"/>
                    <Button x:Name="BtnTemplate" Content="🔍 구조 분석" Style="{StaticResource ToolBtn}" Click="BtnTemplate_Click" IsEnabled="False" Margin="4,0,0,0"/>
                </StackPanel>

                <!-- 오른쪽: 검색바 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="검색" VerticalAlignment="Center" Foreground="#888888" FontSize="12" Margin="0,0,4,0"/>
                    <TextBox x:Name="TxtSearch" Width="200" Style="{StaticResource SearchBox}"
                             KeyDown="TxtSearch_KeyDown"/>
                    <ComboBox x:Name="CboSearchMode" Width="70" SelectedIndex="0" VerticalAlignment="Center" Margin="4,0,0,0">
                        <ComboBoxItem Content="HEX"/>
                        <ComboBoxItem Content="ASCII"/>
                        <ComboBoxItem Content="Regex"/>
                    </ComboBox>
                    <Button Content="▲" Style="{StaticResource ToolBtn}" Padding="8,5" Click="BtnSearchPrev_Click" Margin="4,0,0,0"/>
                    <Button Content="▼" Style="{StaticResource ToolBtn}" Padding="8,5" Click="BtnSearchNext_Click" Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── 컬럼 헤더 ─────────────────────────────────────────────────── -->
        <Border Grid.Row="1"
                Background="#1E1E1E"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,0,0,1"
                Padding="8,3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Offset"
                           Foreground="#555555" FontFamily="Cascadia Code,Consolas" FontSize="11"/>
                <TextBlock Grid.Column="2"
                           Text="00 01 02 03 04 05 06 07  08 09 0A 0B 0C 0D 0E 0F              0123456789ABCDEF"
                           Foreground="#555555" FontFamily="Cascadia Code,Consolas" FontSize="11"/>
            </Grid>
        </Border>

        <!-- ── 헥스 뷰 + 구조체 사이드패널 ─────────────────────────────── -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="ColStruct" Width="0"/>   <!-- 구조체 패널 (토글) -->
            </Grid.ColumnDefinitions>

            <!-- 헥스 ListBox -->
            <ListBox x:Name="HexList"
                     Grid.Column="0"
                     Background="#1A1A1A"
                     BorderThickness="0"
                     ItemTemplate="{StaticResource HexRowTemplate}"
                     ItemContainerStyle="{StaticResource HexItemStyle}"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     SelectionMode="Single"
                     SelectionChanged="HexList_SelectionChanged"
                     MouseDoubleClick="HexList_MouseDoubleClick"
                     HorizontalContentAlignment="Left"
                     Padding="0,4">
                <!-- 드롭 안내 오버레이 -->
                <ListBox.Style>
                    <Style TargetType="ListBox">
                        <Style.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBox">
                                            <Border Background="#1A1A1A">
                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock Text="📁" FontSize="48" HorizontalAlignment="Center" Opacity="0.3" Margin="0,0,0,12"/>
                                                    <TextBlock Text="파일을 드래그하거나 [📂 열기]를 클릭하세요"
                                                               Foreground="#555555" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                                    <TextBlock Text="지원: 모든 바이너리 파일 (50MB 이상은 읽기 전용)"
                                                               Foreground="#444444" FontSize="12" HorizontalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Style>
            </ListBox>

            <!-- 구조체 패널 -->
            <Border Grid.Column="1"
                    Background="#1E1E1E"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#252526" Padding="8,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="TxtFormatName" Text="—" Foreground="#0097C8" FontWeight="Bold" FontSize="12" Margin="0,0,8,0"/>
                            <TextBlock Text="구조체 필드" Foreground="#888888" FontSize="12"/>
                        </StackPanel>
                    </Border>

                    <ListView x:Name="LvFields"
                              Grid.Row="1"
                              Background="Transparent"
                              BorderThickness="0"
                              Foreground="#E0E0E0"
                              FontSize="11"
                              SelectionChanged="LvFields_SelectionChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="오프셋" Width="65"
                                                DisplayMemberBinding="{Binding OffsetHex}"/>
                                <GridViewColumn Header="크기" Width="45"
                                                DisplayMemberBinding="{Binding Length}"/>
                                <GridViewColumn Header="필드명" Width="110"
                                                DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="설명" Width="160"
                                                DisplayMemberBinding="{Binding Description}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>
        </Grid>

        <!-- ── 디코드 패널 ───────────────────────────────────────────────── -->
        <Border Grid.Row="3"
                x:Name="DecodePanel"
                Background="#252526"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="8,5"
                Visibility="Collapsed">
            <WrapPanel Orientation="Horizontal">
                <!-- Int8 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Int8: "   Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt8" Text="—" Foreground="#CDD6F4" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- UInt8 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="UInt8: "  Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtUInt8" Text="—" Foreground="#CDD6F4" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Int16 LE -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Int16 LE: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt16Le" Text="—" Foreground="#A6E3A1" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Int16 BE -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="BE: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt16Be" Text="—" Foreground="#A6E3A1" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Int32 LE -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Int32 LE: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt32Le" Text="—" Foreground="#89DCEB" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Int32 BE -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="BE: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt32Be" Text="—" Foreground="#89DCEB" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Int64 LE -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Int64 LE: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtInt64Le" Text="—" Foreground="#F38BA8" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Float -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Float: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtFloat" Text="—" Foreground="#FAB387" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
                <!-- Double -->
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="Double: " Foreground="#888888" FontFamily="Consolas" FontSize="12"/>
                    <TextBlock x:Name="TxtDouble" Text="—" Foreground="#FAB387" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <!-- ── 상태바 ────────────────────────────────────────────────────── -->
        <Border Grid.Row="4"
                Background="#1E1E1E"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtStatus" Grid.Column="0"
                           Text="파일을 열거나 여기에 드래그 앤 드롭하세요 | Ctrl+G: 오프셋 이동 | Ctrl+F: 검색"
                           Foreground="#666666" FontSize="11" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtFileInfo" Grid.Column="1"
                           Text="" Foreground="#555555" FontSize="11" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
