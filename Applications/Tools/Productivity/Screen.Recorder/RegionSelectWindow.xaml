<Window x:Class="ScreenRecorder.RegionSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ResizeMode="NoResize"
        Cursor="Cross"
        MouseLeftButtonDown="OnMouseDown"
        MouseMove="OnMouseMove"
        MouseLeftButtonUp="OnMouseUp"
        KeyDown="OnKeyDown">
    <Canvas x:Name="OverlayCanvas">
        <!-- 반투명 오버레이 -->
        <Canvas.Background>
            <SolidColorBrush Color="Black" Opacity="0.3"/>
        </Canvas.Background>

        <!-- 선택 영역 표시용 Border -->
        <Border x:Name="SelectionBorder" Visibility="Collapsed"
                BorderBrush="#E74C3C" BorderThickness="2"
                Background="Transparent"/>

        <!-- 크기 표시 텍스트 -->
        <Border x:Name="SizeLabel" Visibility="Collapsed"
                Background="#CC1A1A2E" CornerRadius="4" Padding="8,4">
            <TextBlock x:Name="SizeText" Foreground="#E0E0E0" FontSize="13" FontFamily="Consolas"/>
        </Border>

        <!-- 안내 텍스트 -->
        <Border x:Name="HintLabel" Background="#CC1A1A2E" CornerRadius="6" Padding="16,10"
                Canvas.Left="50" Canvas.Top="50">
            <StackPanel>
                <TextBlock Text="녹화할 영역을 드래그하세요" Foreground="#E0E0E0" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="ESC: 취소  |  드래그 후 놓기: 영역 확정" Foreground="#888888" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Canvas>
</Window>
