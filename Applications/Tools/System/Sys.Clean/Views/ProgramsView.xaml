<UserControl x:Class="SysClean.Views.ProgramsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1A1A1A">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 상단 통계 배너 -->
        <Border Grid.Row="0" Background="#1E1E2E" Padding="16,12" Margin="16,16,16,0"
                CornerRadius="6" BorderBrush="#2A2A4A" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="설치된 프로그램" FontSize="14" FontWeight="SemiBold" Foreground="#7986CB"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="TbProgramCount" Text="" FontSize="12" Foreground="#888"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
                <TextBlock x:Name="TbTotalSize" Text="" FontSize="12" Foreground="#4FC3F7"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- 검색 -->
        <Border Grid.Row="1" Padding="16,10,16,0">
            <TextBox x:Name="TxtSearch" Padding="8,6"
                     TextChanged="TxtSearch_TextChanged"/>
        </Border>

        <!-- 프로그램 목록 -->
        <ListView x:Name="ProgramList" Grid.Row="2" Margin="16,8,16,0"
                  Background="#1E1E1E" BorderThickness="1" BorderBrush="#333"
                  GridViewColumnHeader.Click="OnColumnHeaderClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="250">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Content="프로그램 이름" Tag="Name"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"
                                           FontWeight="SemiBold" Margin="4,0"
                                           ToolTip="{Binding Name}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="170">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Content="제조사" Tag="Publisher"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Publisher}" Foreground="#888"
                                           TextTrimming="CharacterEllipsis" Margin="4,0"
                                           ToolTip="{Binding Publisher}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Content="버전" Tag="Version"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Version}" Foreground="#888"
                                           FontFamily="Consolas" FontSize="11" Margin="4,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="90">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Content="크기" Tag="SizeBytes"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SizeText}" Foreground="#4FC3F7"
                                           HorizontalAlignment="Right" Margin="0,0,8,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <GridViewColumnHeader Content="설치일" Tag="InstallDate"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding InstallDate}" Foreground="#888"
                                           FontSize="11" Margin="4,0"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- 하단 -->
        <Border Grid.Row="3" Background="#1E1E1E" BorderBrush="#333" BorderThickness="0,1,0,0"
                Padding="16,10" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" x:Name="TbStatus" Text="프로그램을 선택하세요."
                           Foreground="#888" FontSize="12" VerticalAlignment="Center"/>

                <Button Grid.Column="1" Content="🔄  새로고침" Click="BtnRefresh_Click"
                        Padding="10,6" Margin="0,0,6,0" FontSize="12"/>

                <Button Grid.Column="2" x:Name="BtnUninstall" Content="🗑  제거"
                        Click="BtnUninstall_Click" Padding="14,7" FontSize="13"
                        Background="#3A1A1A" Foreground="#EF5350" BorderBrush="#C62828"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
