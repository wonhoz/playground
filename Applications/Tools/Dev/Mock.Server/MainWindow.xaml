<Window x:Class="MockServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mock.Server" Height="720" Width="1100"
        MinHeight="500" MinWidth="800"
        Background="#0D0D16"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── 헤더 툴바 ── -->
        <Border Grid.Row="0"
                Background="#131321"
                BorderBrush="#2A2A3D" BorderThickness="0,0,0,1"
                Padding="10,7">
            <DockPanel>
                <!-- 타이틀 -->
                <TextBlock DockPanel.Dock="Left"
                           Text="Mock.Server"
                           Foreground="#818CF8" FontSize="15" FontWeight="Bold"
                           VerticalAlignment="Center" Margin="0,0,20,0"/>

                <!-- 오른쪽: 포트 + 서버 시작/중지 -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock Text="포트:" Foreground="#888898" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox x:Name="TxtPort" Text="8080" Width="65" Height="28"
                             VerticalContentAlignment="Center"
                             ToolTip="서버 리스닝 포트"/>
                    <Button x:Name="BtnToggle" Content="▶ 서버 시작"
                            Width="100" Height="28" Margin="8,0,0,0"
                            Click="BtnToggle_Click"
                            ToolTip="서버를 시작하거나 중지합니다"/>
                </StackPanel>

                <!-- 중앙: 파일 작업 + 포맷 선택 -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnOpen"  Content="📂 열기"   Width="75" Height="28"
                            Style="{StaticResource SecondaryBtn}"
                            Click="BtnOpen_Click" Margin="0,0,6,0"
                            ToolTip="YAML/JSON 파일 열기"/>
                    <Button x:Name="BtnSave"  Content="💾 저장"   Width="75" Height="28"
                            Style="{StaticResource SecondaryBtn}"
                            Click="BtnSave_Click" Margin="0,0,6,0"
                            ToolTip="현재 편집 내용을 파일로 저장"/>
                    <Button x:Name="BtnNew"   Content="📄 새로 만들기" Width="100" Height="28"
                            Style="{StaticResource SecondaryBtn}"
                            Click="BtnNew_Click" Margin="0,0,14,0"
                            ToolTip="기본 YAML 템플릿으로 초기화"/>
                    <TextBlock Text="포맷:" Foreground="#888898" FontSize="12"
                               VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="CbFormat" Width="80" Height="28"
                              SelectionChanged="CbFormat_SelectionChanged">
                        <ComboBoxItem Content="YAML" IsSelected="True"/>
                        <ComboBoxItem Content="JSON"/>
                    </ComboBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ── 본문: 에디터 | 라우트 테이블 + 로그 ── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- ── 열0: 에디터 패널 ── -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 에디터 헤더 -->
                <Border Grid.Row="0"
                        Background="#131321"
                        BorderBrush="#2A2A3D" BorderThickness="0,0,0,1"
                        Padding="10,6">
                    <TextBlock Text="라우트 정의 (YAML/JSON)"
                               Foreground="#888898" FontSize="11"/>
                </Border>

                <!-- 에디터 TextBox -->
                <TextBox x:Name="TxtEditor"
                         Grid.Row="1"
                         FontFamily="Consolas" FontSize="12"
                         AcceptsReturn="True" AcceptsTab="True"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         TextWrapping="NoWrap"
                         Background="#0D0D16" Foreground="#C8C8E8"
                         CaretBrush="#818CF8"
                         BorderThickness="0"
                         Padding="10,8"
                         TextChanged="TxtEditor_TextChanged"/>

                <!-- 에디터 하단: 상태 + 적용 버튼 + 라우트 수 -->
                <Border Grid.Row="2"
                        Background="#131321"
                        BorderBrush="#2A2A3D" BorderThickness="0,1,0,0"
                        Padding="10,5">
                    <DockPanel>
                        <Button x:Name="BtnApply" Content="적용" DockPanel.Dock="Right"
                                Width="55" Height="24" FontSize="11"
                                Click="BtnApply_Click"
                                ToolTip="에디터 내용을 파싱하여 라우트에 반영"/>
                        <TextBlock x:Name="TxtRouteCount" DockPanel.Dock="Right"
                                   Foreground="#888898" FontSize="11"
                                   VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock x:Name="TxtValidation"
                                   Foreground="#22C55E" FontSize="11"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- ── 열1: GridSplitter (수직) ── -->
            <GridSplitter Grid.Column="1"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"/>

            <!-- ── 열2: 라우트 테이블 + 로그 ── -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="55*" MinHeight="100"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="45*" MinHeight="80"/>
                </Grid.RowDefinitions>

                <!-- 라우트 테이블 패널 -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Background="#131321"
                            BorderBrush="#2A2A3D" BorderThickness="0,0,0,1"
                            Padding="10,6">
                        <TextBlock Text="파싱된 라우트"
                                   Foreground="#888898" FontSize="11"/>
                    </Border>

                    <ListBox x:Name="LbRoutes"
                             Grid.Row="1"
                             Background="#0D0D16"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,5" BorderBrush="#2A2A3D" BorderThickness="0,0,0,1">
                                    <DockPanel>
                                        <!-- Method 배지 -->
                                        <Border DockPanel.Dock="Left"
                                                Background="#1A1A35"
                                                BorderBrush="#818CF8" BorderThickness="1"
                                                CornerRadius="3" Padding="5,2"
                                                Margin="0,0,8,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Method}"
                                                       Foreground="#818CF8" FontSize="11"
                                                       FontWeight="Bold" FontFamily="Consolas"/>
                                        </Border>
                                        <!-- Status 배지 -->
                                        <Border DockPanel.Dock="Right"
                                                Background="#1A1A35"
                                                CornerRadius="3" Padding="5,2"
                                                Margin="8,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Status}"
                                                       Foreground="#A5B4FC" FontSize="11"
                                                       FontFamily="Consolas"/>
                                        </Border>
                                        <!-- Delay 배지 -->
                                        <Border DockPanel.Dock="Right"
                                                Background="#1A1A2E"
                                                CornerRadius="3" Padding="5,2"
                                                VerticalAlignment="Center">
                                            <TextBlock FontSize="11" FontFamily="Consolas"
                                                       Foreground="#888898">
                                                <Run Text="{Binding Delay}"/><Run Text="ms"/>
                                            </TextBlock>
                                        </Border>
                                        <!-- Path -->
                                        <TextBlock Text="{Binding Path}"
                                                   Foreground="#C8C8E8" FontSize="12"
                                                   FontFamily="Consolas"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </DockPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>

                <!-- GridSplitter (수평) -->
                <GridSplitter Grid.Row="1"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"/>

                <!-- 요청 로그 패널 -->
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Background="#131321"
                            BorderBrush="#2A2A3D" BorderThickness="0,0,0,1"
                            Padding="10,5">
                        <DockPanel>
                            <Button x:Name="BtnClearLog" Content="🗑 지우기"
                                    DockPanel.Dock="Right"
                                    Style="{StaticResource SecondaryBtn}"
                                    Width="75" Height="22" FontSize="11"
                                    Click="BtnClearLog_Click"/>
                            <TextBlock x:Name="TxtServerStatus"
                                       DockPanel.Dock="Right"
                                       Foreground="#888898" FontSize="11"
                                       VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBlock Text="요청 로그"
                                       Foreground="#888898" FontSize="11"
                                       VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>

                    <ListBox x:Name="LbLog"
                             Grid.Row="1"
                             Background="#0D0D16"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           FontFamily="Consolas" FontSize="11"
                                           Foreground="#C8C8E8" Padding="8,3"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
