<Window x:Class="ImgCast.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Img.Cast"
        Width="480" Height="560"
        MinWidth="480" MinHeight="560"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        Drop="Window_Drop"
        DragEnter="Window_DragEnter"
        DragOver="Window_DragOver"
        Loaded="Window_Loaded">

  <Window.Resources>

    <!-- ── 공통 색상 ── -->
    <SolidColorBrush x:Key="BgBrush"        Color="#1A1A1E"/>
    <SolidColorBrush x:Key="SurfaceBrush"   Color="#252530"/>
    <SolidColorBrush x:Key="Surface2Brush"  Color="#2E2E3A"/>
    <SolidColorBrush x:Key="BorderBrush"    Color="#3A3A48"/>
    <SolidColorBrush x:Key="TextBrush"      Color="#E0E0E0"/>
    <SolidColorBrush x:Key="SubTextBrush"   Color="#888899"/>
    <SolidColorBrush x:Key="AccentBrush"    Color="#6E8EFA"/>
    <SolidColorBrush x:Key="AccentDimBrush" Color="#3A4A8A"/>
    <SolidColorBrush x:Key="DangerBrush"    Color="#FA6E6E"/>

    <!-- ── ComboBox ControlTemplate ── -->
    <ControlTemplate x:Key="DarkComboBoxTemplate" TargetType="ComboBox">
      <Grid>
        <ToggleButton x:Name="ToggleButton"
                      Focusable="False"
                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                      ClickMode="Press">
          <ToggleButton.Template>
            <ControlTemplate TargetType="ToggleButton">
              <Border x:Name="Border"
                      Background="{StaticResource SurfaceBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1"
                      CornerRadius="6">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="28"/>
                  </Grid.ColumnDefinitions>
                  <ContentPresenter Grid.Column="0"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"/>
                  <Path Grid.Column="1"
                        Data="M 0 0 L 5 5 L 10 0"
                        Stroke="{StaticResource SubTextBrush}"
                        StrokeThickness="1.5"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                  <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </ToggleButton.Template>
        </ToggleButton>
        <ContentPresenter x:Name="ContentSite"
                          IsHitTestVisible="False"
                          Content="{TemplateBinding SelectionBoxItem}"
                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                          Margin="10,0,30,0"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Left"/>
        <Popup x:Name="PART_Popup"
               Placement="Bottom"
               IsOpen="{TemplateBinding IsDropDownOpen}"
               AllowsTransparency="True"
               Focusable="False"
               PopupAnimation="Slide">
          <Border Background="{StaticResource Surface2Brush}"
                  BorderBrush="{StaticResource BorderBrush}"
                  BorderThickness="1"
                  CornerRadius="6"
                  MinWidth="{TemplateBinding ActualWidth}"
                  MaxHeight="200"
                  Margin="0,2,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
              <ItemsPresenter/>
            </ScrollViewer>
          </Border>
        </Popup>
      </Grid>
    </ControlTemplate>

    <Style TargetType="ComboBox">
      <Setter Property="Height" Value="34"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Template" Value="{StaticResource DarkComboBoxTemplate}"/>
    </Style>

    <Style TargetType="ComboBoxItem">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    Padding="{TemplateBinding Padding}"
                    CornerRadius="4"
                    Margin="3,1">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentDimBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentDimBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── CheckBox ControlTemplate ── -->
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal">
              <Border x:Name="Box"
                      Width="16" Height="16"
                      Background="{StaticResource SurfaceBrush}"
                      BorderBrush="{StaticResource BorderBrush}"
                      BorderThickness="1.5"
                      CornerRadius="3"
                      VerticalAlignment="Center"
                      Margin="0,0,7,0">
                <Path x:Name="Check"
                      Data="M 2 8 L 6 12 L 14 4"
                      Stroke="{StaticResource AccentBrush}"
                      StrokeThickness="2"
                      Visibility="Collapsed"/>
              </Border>
              <ContentPresenter VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── ProgressBar ── -->
    <Style TargetType="ProgressBar">
      <Setter Property="Height" Value="6"/>
      <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ProgressBar">
            <Border Background="{TemplateBinding Background}"
                    CornerRadius="3">
              <Border x:Name="PART_Track">
                <Border x:Name="PART_Indicator"
                        Background="{TemplateBinding Foreground}"
                        HorizontalAlignment="Left"
                        CornerRadius="3"/>
              </Border>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── Slider ── -->
    <Style TargetType="Slider">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Slider">
            <Grid>
              <Track x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="Slider.DecreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{StaticResource AccentBrush}" Height="4" CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb>
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Ellipse Width="14" Height="14"
                                 Fill="{StaticResource AccentBrush}"
                                 Stroke="{StaticResource SurfaceBrush}"
                                 StrokeThickness="2"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="Slider.IncreaseLarge">
                    <RepeatButton.Template>
                      <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{StaticResource BorderBrush}" Height="4" CornerRadius="2"/>
                      </ControlTemplate>
                    </RepeatButton.Template>
                  </RepeatButton>
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── ScrollBar ── -->
    <Style TargetType="ScrollBar">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid>
              <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.Thumb>
                  <Thumb>
                    <Thumb.Template>
                      <ControlTemplate TargetType="Thumb">
                        <Border Background="{StaticResource BorderBrush}"
                                CornerRadius="3"
                                Margin="2"/>
                      </ControlTemplate>
                    </Thumb.Template>
                  </Thumb>
                </Track.Thumb>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

  </Window.Resources>

  <Grid Background="{StaticResource BgBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>   <!-- 헤더 -->
      <RowDefinition Height="Auto"/>   <!-- 포맷 선택 -->
      <RowDefinition Height="Auto"/>   <!-- 옵션 -->
      <RowDefinition Height="*"/>      <!-- 드롭 존 -->
      <RowDefinition Height="Auto"/>   <!-- 진행 상황 -->
      <RowDefinition Height="Auto"/>   <!-- 상태 텍스트 -->
    </Grid.RowDefinitions>

    <!-- ── 헤더 ── -->
    <Border Grid.Row="0"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,0,0,1"
            Padding="20,14">
      <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <TextBlock Text="🎨"
                   FontSize="22"
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"/>
        <StackPanel>
          <TextBlock Text="Img.Cast"
                     Foreground="{StaticResource TextBrush}"
                     FontSize="18"
                     FontWeight="SemiBold"/>
          <TextBlock Text="이미지 포맷 변환기"
                     Foreground="{StaticResource SubTextBrush}"
                     FontSize="11"/>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- ── 포맷 선택 ── -->
    <Border Grid.Row="1"
            Padding="20,14"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,0,0,1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
          <TextBlock Text="입력 포맷"
                     Foreground="{StaticResource SubTextBrush}"
                     FontSize="11"
                     Margin="0,0,0,5"/>
          <ComboBox x:Name="CbInput" SelectionChanged="CbInput_SelectionChanged">
            <ComboBoxItem Content="자동 감지 (전체)" Tag="All" IsSelected="True"/>
            <ComboBoxItem Content="SVG"  Tag="SVG"/>
            <ComboBoxItem Content="PNG"  Tag="PNG"/>
            <ComboBoxItem Content="JPG"  Tag="JPG"/>
            <ComboBoxItem Content="BMP"  Tag="BMP"/>
          </ComboBox>
        </StackPanel>

        <TextBlock Grid.Column="1"
                   Text="→"
                   Foreground="{StaticResource SubTextBrush}"
                   FontSize="20"
                   VerticalAlignment="Bottom"
                   Margin="14,0,14,6"/>

        <StackPanel Grid.Column="2">
          <TextBlock Text="출력 포맷"
                     Foreground="{StaticResource SubTextBrush}"
                     FontSize="11"
                     Margin="0,0,0,5"/>
          <ComboBox x:Name="CbOutput" SelectionChanged="CbOutput_SelectionChanged">
            <ComboBoxItem Content="ICO (멀티사이즈)" Tag="ICO" IsSelected="True"/>
            <ComboBoxItem Content="PNG"  Tag="PNG"/>
            <ComboBoxItem Content="JPG"  Tag="JPG"/>
            <ComboBoxItem Content="BMP"  Tag="BMP"/>
          </ComboBox>
        </StackPanel>
      </Grid>
    </Border>

    <!-- ── 옵션 ── -->
    <Border Grid.Row="2"
            Padding="20,12"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,0,0,1">
      <StackPanel>
        <!-- JPG 품질 (JPG 출력 시만 표시) -->
        <StackPanel x:Name="JpgQualityPanel" Visibility="Collapsed" Margin="0,0,0,10">
          <Grid Margin="0,0,0,6">
            <TextBlock Text="JPG 품질"
                       Foreground="{StaticResource SubTextBrush}"
                       FontSize="11"
                       VerticalAlignment="Center"/>
            <TextBlock x:Name="TbQualityVal"
                       Text="95"
                       Foreground="{StaticResource AccentBrush}"
                       FontSize="12"
                       FontFamily="Consolas"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"/>
          </Grid>
          <Slider x:Name="SliderQuality"
                  Minimum="10" Maximum="100" Value="95"
                  TickFrequency="5"
                  ValueChanged="SliderQuality_ValueChanged"/>
        </StackPanel>

        <!-- 덮어쓰기 옵션 -->
        <CheckBox x:Name="ChkOverwrite"
                  Content="기존 파일 덮어쓰기"
                  IsChecked="True"/>
      </StackPanel>
    </Border>

    <!-- ── 드롭 존 ── -->
    <Border Grid.Row="3"
            Margin="20,16"
            x:Name="DropZone"
            Background="{StaticResource SurfaceBrush}"
            CornerRadius="10">
      <Border.BorderBrush>
        <SolidColorBrush x:Name="DropBorderBrush" Color="#3A3A48"/>
      </Border.BorderBrush>
      <Border.BorderThickness>
        <Thickness>1.5</Thickness>
      </Border.BorderThickness>

      <!-- 점선 효과 근사: 내부 Border -->
      <Grid>
        <StackPanel x:Name="DropHint"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
          <TextBlock x:Name="DropIcon"
                     Text="📂"
                     FontSize="48"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,12"/>
          <TextBlock Text="파일 또는 폴더를 여기에 드롭"
                     Foreground="{StaticResource TextBrush}"
                     FontSize="15"
                     FontWeight="Medium"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,6"/>
          <TextBlock Text="SVG · PNG · JPG · BMP 지원"
                     Foreground="{StaticResource SubTextBrush}"
                     FontSize="12"
                     HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- 파일 로드 후 목록 요약 -->
        <StackPanel x:Name="FileSummary"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
          <TextBlock x:Name="TbFileCount"
                     Foreground="{StaticResource AccentBrush}"
                     FontSize="32"
                     FontWeight="Bold"
                     FontFamily="Consolas"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,6"/>
          <TextBlock x:Name="TbFileLabel"
                     Foreground="{StaticResource TextBrush}"
                     FontSize="14"
                     HorizontalAlignment="Center"
                     Margin="0,0,0,4"/>
          <TextBlock x:Name="TbFileSample"
                     Foreground="{StaticResource SubTextBrush}"
                     FontSize="11"
                     HorizontalAlignment="Center"
                     TextWrapping="Wrap"
                     MaxWidth="300"
                     TextAlignment="Center"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- ── ProgressBar ── -->
    <Grid Grid.Row="4" Margin="20,0,20,8">
      <ProgressBar x:Name="PBar"
                   Minimum="0" Maximum="100" Value="0"
                   Visibility="Collapsed"/>
    </Grid>

    <!-- ── 상태 텍스트 ── -->
    <Border Grid.Row="5"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,1,0,0"
            Padding="20,8">
      <TextBlock x:Name="TbStatus"
                 Text="파일을 드롭하면 자동으로 변환을 시작합니다"
                 Foreground="{StaticResource SubTextBrush}"
                 FontSize="11"
                 TextTrimming="CharacterEllipsis"/>
    </Border>
  </Grid>
</Window>
