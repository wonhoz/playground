<Window x:Class="ClipboardStacker.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clipboard Stacker"
        Width="420" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <!-- 공통 버튼 -->
        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground"      Value="#888899"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2A2A40"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 스택 항목 버튼 -->
        <Style x:Key="ClipItemBtn" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Padding"         Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1E1E36"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 변환 토글 버튼 -->
        <Style x:Key="TransformBtn" TargetType="ToggleButton">
            <Setter Property="Background"      Value="#1A1A2E"/>
            <Setter Property="Foreground"      Value="#888899"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush"     Value="#2A2A44"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Padding"         Value="10,4"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background"   Value="#2A2A4A"/>
                                <Setter TargetName="bd" Property="BorderBrush"  Value="#4FC3F7"/>
                                <Setter Property="Foreground" Value="#4FC3F7"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#222236"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 팝업 본체 -->
    <Border Background="#12121E" CornerRadius="12" BorderBrush="#252540" BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.6" ShadowDepth="4" Color="#000000"/>
        </Border.Effect>

        <StackPanel>
            <!-- ── 헤더 ── -->
            <Grid Margin="16,12,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="📋 Clipboard Stacker" FontSize="14" FontWeight="Bold"
                           Foreground="#4FC3F7" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="✕" Style="{StaticResource IconBtn}"
                        FontSize="14" Click="CloseBtn_Click"/>
            </Grid>

            <!-- ── 섹션: 스택 ── -->
            <Grid Margin="16,10,16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StackHeader" Text="스택 (0개)"
                           FontSize="11" Foreground="#555577" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="모두 지우기" Style="{StaticResource IconBtn}"
                        FontSize="10" Click="ClearStack_Click"/>
            </Grid>

            <!-- 스택 목록 -->
            <Border Margin="12,4,12,0" Background="#0E0E1E" CornerRadius="8" Padding="4">
                <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="StackPanel"/>
                </ScrollViewer>
            </Border>

            <!-- 비어있을 때 안내 -->
            <TextBlock x:Name="EmptyHint" Text="복사한 텍스트가 없습니다. Ctrl+C로 복사해 보세요."
                       FontSize="11" Foreground="#333355" HorizontalAlignment="Center"
                       Margin="0,10,0,0" Visibility="Collapsed"/>

            <!-- ── 섹션: 즐겨찾기 ── -->
            <TextBlock Text="⭐ 즐겨찾기" FontSize="11" Foreground="#555577"
                       Margin="16,12,16,0"/>
            <Border Margin="12,4,12,0" Background="#0E0E1E" CornerRadius="8" Padding="4">
                <StackPanel x:Name="PinnedPanel"/>
            </Border>
            <TextBlock x:Name="NoPinnedHint" Text="항목에서 📌을 클릭하면 즐겨찾기로 고정됩니다."
                       FontSize="11" Foreground="#333355" HorizontalAlignment="Center"
                       Margin="0,6,0,0" Visibility="Visible"/>

            <!-- ── 텍스트 변환 ── -->
            <Grid Margin="12,10,12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="변환:" FontSize="11" Foreground="#555577"
                           VerticalAlignment="Center" Margin="4,0,8,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
                    <ToggleButton x:Name="BtnNone"  Content="없음" Style="{StaticResource TransformBtn}"
                                  Margin="0,0,4,0" IsChecked="True" Click="Transform_Click" Tag="None"/>
                    <ToggleButton x:Name="BtnUpper" Content="UPPER" Style="{StaticResource TransformBtn}"
                                  Margin="0,0,4,0" Click="Transform_Click" Tag="Upper"/>
                    <ToggleButton x:Name="BtnLower" Content="lower" Style="{StaticResource TransformBtn}"
                                  Margin="0,0,4,0" Click="Transform_Click" Tag="Lower"/>
                    <ToggleButton x:Name="BtnTrim"  Content="Trim" Style="{StaticResource TransformBtn}"
                                  Click="Transform_Click" Tag="Trim"/>
                </StackPanel>
            </Grid>

            <!-- ── 도움말 ── -->
            <Border Margin="12,10,12,12" Background="#0A0A18" CornerRadius="6" Padding="10,6">
                <TextBlock FontSize="10" Foreground="#333355" TextWrapping="Wrap">
                    <Run Text="Ctrl+Shift+V"/>
                    <Run Text=" — 스택에서 순서대로 붙여넣기  |  항목 클릭 — 즉시 붙여넣기"/>
                </TextBlock>
            </Border>
        </StackPanel>
    </Border>
</Window>
