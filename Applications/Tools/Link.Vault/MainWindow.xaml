<Window x:Class="LinkVault.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Link.Vault — 오프라인 북마크" Width="1200" Height="780"
        MinWidth="900" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F1A"
        Closing="Window_Closing">

    <Window.Resources>
        <!-- 버튼 -->
        <Style x:Key="DarkBtn" TargetType="Button">
            <Setter Property="Background" Value="#252540"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#303058"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentBtn" TargetType="Button" BasedOn="{StaticResource DarkBtn}">
            <Setter Property="Background" Value="#2050A0"/>
        </Style>

        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource DarkBtn}">
            <Setter Property="Background" Value="#802020"/>
        </Style>

        <!-- 텍스트박스 -->
        <Style x:Key="DarkTB" TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#333350"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="#4080FF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 목록 아이템 -->
        <Style x:Key="BookmarkItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="#252540" BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A1A30"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#202050"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#4060C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 태그 버튼 -->
        <Style x:Key="TagBtn" TargetType="Button">
            <Setter Property="Background" Value="#1E2040"/>
            <Setter Property="Foreground" Value="#8090C0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,2,4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2A3060"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TagBtnActive" TargetType="Button" BasedOn="{StaticResource TagBtn}">
            <Setter Property="Background" Value="#2A3A80"/>
            <Setter Property="Foreground" Value="#A0C0FF"/>
        </Style>

        <!-- 체크박스: App.xaml 전역 스타일 사용 (ControlTemplate 포함)
             Window.Resources 로컬 스타일이 App.xaml을 덮어쓰므로 삭제 처리 -->
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── 헤더 ── -->
        <Border Grid.Row="0" Background="#141428" Padding="14,10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <TextBlock Text="🔗" FontSize="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Link.Vault" FontSize="18" FontWeight="Bold" Foreground="#4A8FFF"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtCount" Text="" FontSize="11" Foreground="#60608A"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnAdd" Content="＋ 북마크 추가" Style="{StaticResource AccentBtn}"
                            Click="BtnAdd_Click" Margin="0,0,6,0"/>
                    <Button x:Name="BtnRefresh" Content="🔄 새로고침" Style="{StaticResource DarkBtn}"
                            Click="BtnRefresh_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ── 검색/필터 바 ── -->
        <Border Grid.Row="1" Background="#111120" Padding="14,8">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="10,0,0,0">
                    <CheckBox x:Name="ChkUnread" Content="미읽음" Margin="0,0,12,0"
                              Checked="Filter_Changed" Unchecked="Filter_Changed"
                              VerticalContentAlignment="Center"/>
                    <TextBlock Text="별점≥" Foreground="#888" FontSize="12" VerticalAlignment="Center"/>
                    <ComboBox x:Name="CmbStars" Width="52" Margin="4,0,0,0"
                              Background="#1A1A2E" Foreground="#E0E0E0" FontSize="12"
                              BorderBrush="#333350" VerticalContentAlignment="Center"
                              SelectionChanged="Filter_Changed">
                        <ComboBoxItem Content="전체" IsSelected="True"/>
                        <ComboBoxItem Content="★1"/>
                        <ComboBoxItem Content="★2"/>
                        <ComboBoxItem Content="★3"/>
                        <ComboBoxItem Content="★4"/>
                        <ComboBoxItem Content="★5"/>
                    </ComboBox>
                </StackPanel>
                <TextBox x:Name="TxtSearch" Style="{StaticResource DarkTB}"
                         TextChanged="TxtSearch_TextChanged">
                    <TextBox.Tag>검색 (제목, 설명, 태그 전문 검색...)</TextBox.Tag>
                </TextBox>
            </DockPanel>
        </Border>

        <!-- ── 메인 레이아웃 ── -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" MinWidth="160" MaxWidth="340"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="320" MinWidth="240" MaxWidth="480"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 태그 패널 -->
            <Grid Grid.Column="0" Background="#0D0D1C">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="#141428" Padding="12,8">
                    <TextBlock Text="🏷 태그" Foreground="#6070A0" FontSize="12" FontWeight="SemiBold"/>
                </Border>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <WrapPanel x:Name="TagPanel" Margin="10,8" Orientation="Horizontal"/>
                </ScrollViewer>
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" Background="#0A0A18"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <!-- 북마크 목록 -->
            <Grid Grid.Column="2" Background="#0F0F1C">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Background="#141428" Padding="12,8">
                    <TextBlock x:Name="TxtListHeader" Text="전체" Foreground="#6070A0"
                               FontSize="12" FontWeight="SemiBold"/>
                </Border>
                <ListBox x:Name="LstBookmarks" Grid.Row="1"
                         Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource BookmarkItem}"
                         SelectionChanged="LstBookmarks_SelectionChanged"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <!-- 제목 -->
                                <DockPanel Grid.Row="0">
                                    <TextBlock Text="{Binding StarsDisplay}" FontSize="9"
                                               Foreground="#C0A030" VerticalAlignment="Center"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding ReadStatusDisplay}" FontSize="9"
                                               Foreground="#505580" VerticalAlignment="Center"
                                               Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold"
                                               Foreground="#D8D8F0" TextTrimming="CharacterEllipsis"/>
                                </DockPanel>
                                <!-- 호스트 + 날짜 -->
                                <DockPanel Grid.Row="1" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding Host}" FontSize="10" Foreground="#5070A0"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:MM/dd}}"
                                               FontSize="10" Foreground="#404060"
                                               DockPanel.Dock="Right" HorizontalAlignment="Right"/>
                                </DockPanel>
                                <!-- 태그 -->
                                <TextBlock Grid.Row="2" Text="{Binding Tags}" FontSize="10"
                                           Foreground="#405080" Margin="0,2,0,0"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <GridSplitter Grid.Column="3" Width="5" Background="#0A0A18"
                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <!-- 상세 + 미리보기 -->
            <Grid Grid.Column="4" Background="#0C0C1A">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 상세 정보 -->
                <Border Grid.Row="0" Background="#141428" Padding="14,10"
                        x:Name="DetailPanel" Visibility="Collapsed">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 제목 + 버튼들 -->
                        <DockPanel Grid.Row="0">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button x:Name="BtnToggleRead" Content="읽음 표시" Style="{StaticResource DarkBtn}"
                                        Click="BtnToggleRead_Click" Padding="8,4" Margin="0,0,4,0" FontSize="11"/>
                                <Button x:Name="BtnSnapshot" Content="📥 스냅샷" Style="{StaticResource DarkBtn}"
                                        Click="BtnSnapshot_Click" Padding="8,4" Margin="0,0,4,0" FontSize="11"/>
                                <Button x:Name="BtnEdit" Content="✏ 편집" Style="{StaticResource DarkBtn}"
                                        Click="BtnEdit_Click" Padding="8,4" Margin="0,0,4,0" FontSize="11"/>
                                <Button x:Name="BtnDelete" Content="🗑 삭제" Style="{StaticResource DangerBtn}"
                                        Click="BtnDelete_Click" Padding="8,4" FontSize="11"/>
                            </StackPanel>
                            <TextBlock x:Name="TxtDetailTitle" FontSize="15" FontWeight="Bold"
                                       Foreground="#D0D0F8" TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"/>
                        </DockPanel>

                        <!-- URL + 별점 -->
                        <DockPanel Grid.Row="1" Margin="0,6,0,0">
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Text="별점: " Foreground="#808090" FontSize="11" VerticalAlignment="Center"/>
                                <Button x:Name="BtnStar1" Content="★" Tag="1" Style="{StaticResource DarkBtn}"
                                        Click="BtnStar_Click" Padding="2,0" FontSize="14" Foreground="#C0A030"/>
                                <Button x:Name="BtnStar2" Content="★" Tag="2" Style="{StaticResource DarkBtn}"
                                        Click="BtnStar_Click" Padding="2,0" FontSize="14" Foreground="#C0A030"/>
                                <Button x:Name="BtnStar3" Content="★" Tag="3" Style="{StaticResource DarkBtn}"
                                        Click="BtnStar_Click" Padding="2,0" FontSize="14" Foreground="#C0A030"/>
                                <Button x:Name="BtnStar4" Content="★" Tag="4" Style="{StaticResource DarkBtn}"
                                        Click="BtnStar_Click" Padding="2,0" FontSize="14" Foreground="#C0A030"/>
                                <Button x:Name="BtnStar5" Content="★" Tag="5" Style="{StaticResource DarkBtn}"
                                        Click="BtnStar_Click" Padding="2,0" FontSize="14" Foreground="#C0A030"/>
                            </StackPanel>
                            <TextBlock x:Name="TxtDetailUrl" FontSize="11" Foreground="#4060A0"
                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                       Cursor="Hand" MouseLeftButtonDown="TxtDetailUrl_Click"/>
                        </DockPanel>

                        <!-- 설명 + 태그 -->
                        <DockPanel Grid.Row="2" Margin="0,4,0,0">
                            <TextBlock x:Name="TxtDetailTags" DockPanel.Dock="Right"
                                       FontSize="11" Foreground="#405070"
                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                            <TextBlock x:Name="TxtDetailDesc" FontSize="11" Foreground="#7070A0"
                                       TextTrimming="CharacterEllipsis"/>
                        </DockPanel>
                    </Grid>
                </Border>

                <!-- 스냅샷 툴바 -->
                <Border Grid.Row="1" Background="#111120" Padding="10,6"
                        x:Name="PreviewToolbar" Visibility="Collapsed">
                    <DockPanel>
                        <Button x:Name="BtnOpenBrowser" Content="🌐 브라우저에서 열기"
                                Style="{StaticResource DarkBtn}" Click="BtnOpenBrowser_Click"
                                DockPanel.Dock="Right" Padding="8,4" FontSize="11"/>
                        <TextBlock x:Name="TxtPreviewStatus" Foreground="#606080" FontSize="11"
                                   VerticalAlignment="Center"/>
                    </DockPanel>
                </Border>

                <!-- WebView2 미리보기 -->
                <Grid Grid.Row="2">
                    <TextBlock x:Name="TxtEmptyHint"
                               Text="← 북마크를 선택하면 미리보기가 표시됩니다"
                               Foreground="#303050" FontSize="14"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <wv2:WebView2 x:Name="WebPreview"
                                  Visibility="Collapsed"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- ── 상태 바 ── -->
        <Border Grid.Row="3" Background="#0E0E20" Padding="14,5">
            <DockPanel>
                <TextBlock x:Name="TxtStatus" Text="준비" Foreground="#404060" FontSize="11"
                           DockPanel.Dock="Left" VerticalAlignment="Center"/>
                <TextBlock x:Name="TxtApiStatus" Text="" Foreground="#304050" FontSize="11"
                           DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
