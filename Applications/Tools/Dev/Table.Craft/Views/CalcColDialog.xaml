<Window x:Class="TableCraft.Views.CalcColDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="계산 컬럼 추가"
        Width="520" Height="340"
        MinWidth="440" MinHeight="280"
        Background="#1E1E2E"
        Foreground="#CDD6F4"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 컬럼 이름 -->
        <TextBlock Grid.Row="0" Text="새 컬럼 이름"
                   Foreground="#A6ADC8" FontSize="11" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="TxtName"
                 Height="30" FontSize="13"
                 Margin="0,0,0,10"/>

        <!-- 수식 -->
        <TextBlock Grid.Row="2" Foreground="#A6ADC8" FontSize="11" Margin="0,0,0,4">
            <Run Text="수식 (예: "/>
            <Run Text="=A+B" Foreground="#A6E3A1" FontFamily="Consolas"/>
            <Run Text=",  "/>
            <Run Text="=UPPER([이름])" Foreground="#A6E3A1" FontFamily="Consolas"/>
            <Run Text=",  "/>
            <Run Text="=IF(A>0,&quot;양수&quot;,&quot;음수&quot;)" Foreground="#A6E3A1" FontFamily="Consolas"/>
            <Run Text=")"/>
        </TextBlock>
        <TextBox Grid.Row="3" x:Name="TxtExpr"
                 Height="60" FontSize="13" FontFamily="Consolas"
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="False"
                 Margin="0,0,0,10"
                 VerticalContentAlignment="Top"/>

        <!-- 컬럼 참조 힌트 -->
        <Border Grid.Row="3" VerticalAlignment="Bottom" Margin="0,0,0,14">
            <StackPanel x:Name="HintPanel" Orientation="Horizontal" Margin="4,2">
                <TextBlock Text="컬럼 참조: " Foreground="#6C7086" FontSize="10"/>
                <ItemsControl x:Name="ColHints">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#313244" CornerRadius="3" Margin="2,0"
                                    Padding="4,1" Cursor="Hand"
                                    MouseLeftButtonDown="ColHint_Click">
                                <TextBlock Text="{Binding}" FontFamily="Consolas"
                                           Foreground="#A6E3A1" FontSize="10"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- 버튼 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="추가" Width="70" Margin="4,0"
                    Click="BtnOk_Click" Background="#313244"/>
            <Button Content="취소" Width="70"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
